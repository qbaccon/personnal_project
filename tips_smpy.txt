php bin/console + ...
	make:entity (entité = objet = classe)
	-> nom de classe
	-> ajout de champ/propriétée
	-> (par champ) type du champ
	-> (par champ) taille
	-> (par champ) NULL ? (BDD)

	make:migration => a faire UNE fois au début

Entity: pour modifier une entité existante re-taper un nom existant

	doctrine:migrations:diff => a faire pour les autres migrations APRES la première
	doctrine:migrations:migrate => a faire pour envoyer les migrations (modif)

CREER BDD
	doctrine:database:create

CREER CONTROLLEUR
	make:controller
	-> nom controller (...Controller)

CREER FORMULAIRE
	make:form
	-> nom formulaire (...Type)

VIDER CACHE SPNY
	cache:clear

SERVICE
	banque de fonction a inclure dans les controller en parametres
	dossier a creer dans src/Service

RECUPERER UN PROJET
	git clone nom_du_repo
	composer install (dans mon_projet)